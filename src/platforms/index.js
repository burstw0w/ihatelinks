const { handleInstagram } = require("./instagram");
const { handleTikTok } = require("./tiktok");
const { handleTwitter } = require("./twitter");
const { handleReddit } = require("./reddit");
const { handlePinterest } = require("./pinterest");

module.exports = {
    handleInstagram,
    handleTikTok,
    handleTwitter,
    handleReddit,
    handlePinterest
};
